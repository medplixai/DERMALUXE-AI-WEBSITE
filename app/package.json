{
  "name": "dermaluxe-staff-app",
  "version": "1.1.0",
  "private": true,
  "description": "DermaLuxe by Medicare \u2014 staff Android app (Capacitor shell around the staff dashboard).",
  "scripts": {
    "shell": "node build-shell.js",
    "sync": "npm run shell && npx cap sync android",
    "apk": "npm run sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/cli": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/push-notifications": "^8.1.2"
  }
}